# Configuration of Academic
# Documentation: https://sourcethemes.com/academic/
#
# This file is formatted using TOML syntax - learn more at https://learnxinyminutes.com/docs/toml/
# Each configuration section is defined by a name in square brackets (e.g. `[outputs]`).

# Title of your site
title = "Tim Trice"

# Set relativeurls to true to prevent missing stylesheets/images when testing
# locally.
relativeurls = true

# The URL of your site.
# End your URL with a `/` trailing slash, e.g. `https://example.com/`.
baseurl = "/"

# Enter a copyright notice to display in the site footer.
# To display a copyright symbol, type `&copy;`. For current year, type `{year}`.
copyright = "&copy; {year}"

# Enable analytics by entering your Google Analytics tracking ID
googleAnalytics = "UA-1507390-26"

# Enable comments by entering your Disqus shortname
disqusShortname = "http-timtrice-net"

preserveTaxonomyNames = true

publishDir = "public"

pluralizeListTitles = "false"

titleCaseStyle = "Chicago"

############################
## Advanced options below ##
############################

# Name of Academic theme folder in `themes/`.
theme = "hugo-academic"

# Get last modified date for content from Git?
enableGitInfo = true

# Default language to use (if you setup multilingual support)
defaultContentLanguage = "en"

hasCJKLanguage = false  # Set `true` for Chinese/Japanese/Korean languages.

defaultContentLanguageInSubdir = false

removePathAccents = true  # Workaround for https://github.com/gohugoio/hugo/issues/5687

paginate = 10  # Number of items per page in paginated lists.

enableEmoji = true

footnotereturnlinkcontents = "<sup>^</sup>"

ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "\\.R$", "_files$", "_cache$", "Dockerfile", "docker-compose.yml", "user-settings"]

[outputs]
  home = [ "HTML", "RSS", "JSON" ]
  section = [ "HTML", "RSS" ]

[permalinks]
  post = "/:year/:month/:day/:slug/"

# Configure BlackFriday Markdown rendering.
#   See: https://gohugo.io/getting-started/configuration/#configure-blackfriday
[blackfriday]
  hrefTargetBlank = true  # `true` opens external links in a new tab. See https://github.com/gohugoio/hugo/issues/2424
	angledQuotes = false
	latexDashes = true
  extensions = ["backslashLineBreak"]

[imaging]
  resampleFilter = "lanczos"
  quality = 90
  anchor = "smart"  # Anchor for cropping. Options include Smart and Center.

# Taxonomies.
[taxonomies]
  tag = "tags"
  category = "categories"
  publication_type = "publication_types"
  author = "authors"
